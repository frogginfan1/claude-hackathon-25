/* ===================================
   Design System Variables
   =================================== */
:root {
    /* Colors - OKLCH */
    --color-bg: oklch(15% 0.01 240);
    --color-surface: oklch(20% 0.02 240);
    --color-surface-light: oklch(25% 0.02 240);
    --color-surface-hover: oklch(28% 0.02 240);
    --color-text-primary: oklch(95% 0.01 240);
    --color-text-secondary: oklch(70% 0.02 240);
    --color-text-muted: oklch(55% 0.02 240);
    
    --color-primary: oklch(75% 0.19 155);
    --color-secondary: oklch(70% 0.15 210);
    --color-warning: oklch(75% 0.15 85);
    --color-success: oklch(70% 0.18 145);
    --color-error: oklch(65% 0.20 25);
    
    /* Typography Scale - Perfect Fourth (1.333) */
    --font-display: 'Orbitron', sans-serif;
    --font-body: 'Inter', sans-serif;
    
    --text-display-xl: clamp(3rem, 6vw, 5rem);
    --text-display-l: clamp(2.25rem, 4vw, 3.5rem);
    --text-display-m: clamp(1.5rem, 2.5vw, 2.25rem);
    --text-body-l: clamp(1.125rem, 1.8vw, 1.375rem);
    --text-body-m: clamp(1rem, 1.4vw, 1.125rem);
    --text-body-s: clamp(0.875rem, 1.2vw, 1rem);
    --text-caption: clamp(0.75rem, 1vw, 0.875rem);
    
    /* Spacing - 1.5 ratio */
    --space-xs: 0.5rem;
    --space-sm: 0.75rem;
    --space-md: 1rem;
    --space-lg: 1.5rem;
    --space-xl: 2.25rem;
    --space-2xl: 3.375rem;
    --space-3xl: 5rem;
    
    /* Layout */
    --max-width: 1400px;
    --quiz-width: 700px;
    --gutter: 1.5rem;
    
    /* Effects */
    --transition-fast: 0.2s ease;
    --transition-medium: 0.4s ease;
    --transition-slow: 0.6s ease;
    
    --glow-primary: 0 0 20px oklch(75% 0.19 155 / 0.3),
                    0 0 40px oklch(75% 0.19 155 / 0.2),
                    0 0 60px oklch(75% 0.19 155 / 0.1);
    
    --glow-secondary: 0 0 20px oklch(70% 0.15 210 / 0.3),
                      0 0 40px oklch(70% 0.15 210 / 0.2);
}

/* ===================================
   Reset & Base Styles
   =================================== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: var(--font-body);
    background: var(--color-bg);
    color: var(--color-text-primary);
    line-height: 1.6;
    overflow-x: hidden;
    min-height: 100vh;
}

/* ===================================
   Page Management
   =================================== */
.page {
    display: none;
    min-height: 100vh;
    opacity: 0;
    transition: opacity var(--transition-medium);
}

.page.active {
    display: flex;
    opacity: 1;
}

/* ===================================
   Landing Page
   =================================== */
#landing-page {
    position: relative;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.hero-container {
    position: relative;
    width: 100%;
    max-width: var(--max-width);
    padding: var(--space-xl);
    text-align: center;
    z-index: 1;
}

/* Animated Background Grid */
.background-grid {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image:
        linear-gradient(oklch(20% 0.02 240 / 0.4) 1px, transparent 1px),
        linear-gradient(90deg, oklch(20% 0.02 240 / 0.4) 1px, transparent 1px);
    background-size: 50px 50px;
    animation: gridScroll 20s linear infinite;
    opacity: 0.5;
}

@keyframes gridScroll {
    0% {
        transform: translate(0, 0);
    }
    100% {
        transform: translate(50px, 50px);
    }
}

/* Glowing Orbs */
.glow-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.3;
    animation: float 8s ease-in-out infinite;
    pointer-events: none;
}

.glow-orb-1 {
    width: 400px;
    height: 400px;
    background: var(--color-primary);
    top: -100px;
    left: -100px;
    animation-delay: 0s;
}

.glow-orb-2 {
    width: 300px;
    height: 300px;
    background: var(--color-secondary);
    bottom: -50px;
    right: -50px;
    animation-delay: 2s;
}

@keyframes float {
    0%, 100% {
        transform: translate(0, 0) scale(1);
    }
    50% {
        transform: translate(30px, 30px) scale(1.1);
    }
}

/* Hero Content */
.hero-content {
    position: relative;
    z-index: 2;
}

.hero-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 80px;
    height: 80px;
    margin-bottom: var(--space-lg);
    font-size: 2.5rem;
    color: var(--color-primary);
    background: var(--color-surface);
    border: 2px solid var(--color-primary);
    clip-path: polygon(30% 0%, 70% 0%, 100% 30%, 100% 70%, 70% 100%, 30% 100%, 0% 70%, 0% 30%);
    box-shadow: var(--glow-primary);
    animation: pulse 3s ease-in-out infinite;
}

@keyframes pulse {
    0%, 100% {
        transform: scale(1);
        box-shadow: var(--glow-primary);
    }
    50% {
        transform: scale(1.05);
        box-shadow: 0 0 30px oklch(75% 0.19 155 / 0.5),
                    0 0 60px oklch(75% 0.19 155 / 0.3),
                    0 0 90px oklch(75% 0.19 155 / 0.1);
    }
}

.hero-title {
    font-family: var(--font-display);
    font-size: var(--text-display-xl);
    font-weight: 900;
    line-height: 1.1;
    margin-bottom: var(--space-lg);
    letter-spacing: -0.02em;
}

.title-line {
    display: block;
}

.title-line.highlight {
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: var(--glow-primary);
}

.hero-subtitle {
    font-size: var(--text-body-l);
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto var(--space-2xl);
    line-height: 1.7;
}

/* CTA Button */
.cta-button {
    position: relative;
    padding: var(--space-lg) var(--space-2xl);
    font-family: var(--font-display);
    font-size: var(--text-body-l);
    font-weight: 700;
    color: var(--color-bg);
    background: var(--color-primary);
    border: none;
    cursor: pointer;
    overflow: hidden;
    clip-path: polygon(8% 0%, 100% 0%, 92% 100%, 0% 100%);
    transition: transform var(--transition-fast);
}

.cta-button:hover {
    transform: translateY(-2px) scale(1.02);
}

.cta-button:active {
    transform: translateY(0) scale(0.98);
}

.button-content {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    position: relative;
    z-index: 2;
}

.button-icon {
    transition: transform var(--transition-fast);
}

.cta-button:hover .button-icon {
    transform: translateX(4px);
}

.button-glow {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--color-text-primary);
    opacity: 0;
    transition: opacity var(--transition-fast);
    pointer-events: none;
}

.cta-button:hover .button-glow {
    opacity: 0.15;
    box-shadow: 0 0 30px var(--color-text-primary);
}

/* ===================================
   Quiz Page
   =================================== */
#quiz-page {
    align-items: center;
    justify-content: center;
    padding: var(--space-xl) var(--gutter);
}

.quiz-container {
    width: 100%;
    max-width: var(--quiz-width);
}

/* Quiz Header */
.quiz-header {
    display: flex;
    flex-direction: column;
    gap: var(--space-lg);
    margin-bottom: var(--space-xl);
}

.progress-container {
    display: flex;
    align-items: center;
    gap: var(--space-md);
}

.progress-bar {
    flex: 1;
    height: 6px;
    background: var(--color-surface);
    border-radius: 10px;
    overflow: hidden;
    position: relative;
}

.progress-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--color-primary), var(--color-secondary));
    border-radius: 10px;
    transition: width var(--transition-medium);
    box-shadow: var(--glow-primary);
}

.progress-text {
    font-family: var(--font-display);
    font-size: var(--text-body-s);
    color: var(--color-text-secondary);
    white-space: nowrap;
}

.progress-text #current-question {
    color: var(--color-primary);
    font-weight: 700;
}

/* Category Badge */
.category-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
    padding: var(--space-sm) var(--space-lg);
    background: var(--color-surface);
    border: 1px solid var(--color-primary);
    clip-path: polygon(6% 0%, 100% 0%, 94% 100%, 0% 100%);
    font-family: var(--font-display);
    font-size: var(--text-body-s);
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: 0.1em;
}

.category-badge i {
    font-size: 1.2em;
}

/* Quiz Content */
.quiz-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-xl);
}

.question-text {
    font-family: var(--font-display);
    font-size: var(--text-display-m);
    font-weight: 700;
    line-height: 1.3;
    color: var(--color-text-primary);
}

/* Answers */
.answers-container {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
}

.answer-option {
    position: relative;
    padding: var(--space-lg);
    background: var(--color-surface);
    border: 2px solid transparent;
    cursor: pointer;
    transition: all var(--transition-fast);
    font-size: var(--text-body-l);
    color: var(--color-text-primary);
    text-align: left;
    overflow: hidden;
}

.answer-option::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: var(--color-primary);
    transform: scaleY(0);
    transition: transform var(--transition-fast);
}

.answer-option:hover {
    background: var(--color-surface-light);
    border-color: var(--color-primary);
    transform: translateX(4px);
}

.answer-option:hover::before {
    transform: scaleY(1);
}

.answer-option.selected {
    background: var(--color-surface-light);
    border-color: var(--color-primary);
    box-shadow: 0 0 20px oklch(75% 0.19 155 / 0.2);
}

.answer-option.selected::before {
    transform: scaleY(1);
}

/* ===================================
   Results Page
   =================================== */
#results-page {
    padding: var(--space-2xl) var(--gutter);
    overflow-y: auto;
}

.results-container {
    width: 100%;
    max-width: var(--max-width);
    margin: 0 auto;
}

.results-header {
    text-align: center;
    margin-bottom: var(--space-3xl);
    padding-bottom: var(--space-2xl);
    border-bottom: 1px solid var(--color-surface-light);
}

.results-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 100px;
    height: 100px;
    margin-bottom: var(--space-xl);
    font-size: 3rem;
    color: var(--color-primary);
    background: var(--color-surface);
    border: 3px solid var(--color-primary);
    clip-path: circle(50%);
    box-shadow: var(--glow-primary);
}

.results-title {
    font-family: var(--font-display);
    font-size: var(--text-display-l);
    font-weight: 900;
    margin-bottom: var(--space-xl);
}

.total-emissions {
    display: inline-block;
    padding: var(--space-lg) var(--space-2xl);
    background: var(--color-surface);
    border: 2px solid var(--color-primary);
    clip-path: polygon(6% 0%, 100% 0%, 94% 100%, 0% 100%);
}

.total-label {
    font-size: var(--text-body-s);
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-xs);
}

.total-value {
    font-family: var(--font-display);
    font-size: var(--text-display-m);
    font-weight: 900;
    color: var(--color-primary);
}

/* Results Breakdown */
.results-breakdown {
    display: grid;
    gap: var(--space-2xl);
}

.category-result {
    background: var(--color-surface);
    padding: var(--space-xl);
    border: 1px solid var(--color-surface-light);
    position: relative;
    overflow: hidden;
}

.category-result::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 4px;
    background: linear-gradient(90deg, var(--color-primary), var(--color-secondary));
}

.category-result-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-xl);
    flex-wrap: wrap;
    gap: var(--space-md);
}

.category-info {
    display: flex;
    align-items: center;
    gap: var(--space-md);
}

.category-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    font-size: 1.5rem;
    color: var(--color-primary);
    background: var(--color-bg);
    border: 2px solid var(--color-primary);
    clip-path: polygon(30% 0%, 70% 0%, 100% 30%, 100% 70%, 70% 100%, 30% 100%, 0% 70%, 0% 30%);
}

.category-name {
    font-family: var(--font-display);
    font-size: var(--text-display-m);
    font-weight: 700;
}

.category-emissions {
    text-align: right;
}

.emissions-label {
    font-size: var(--text-body-s);
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

.emissions-value {
    font-family: var(--font-display);
    font-size: var(--text-body-l);
    font-weight: 700;
    color: var(--color-primary);
}

/* Comparison Bar */
.comparison-bar {
    margin-bottom: var(--space-xl);
}

.comparison-label {
    display: flex;
    justify-content: space-between;
    margin-bottom: var(--space-sm);
    font-size: var(--text-body-s);
    color: var(--color-text-secondary);
}

.bar-container {
    position: relative;
    height: 30px;
    background: var(--color-bg);
    border-radius: 4px;
    overflow: hidden;
}

.bar-average {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    background: oklch(60% 0.05 240);
    transition: width var(--transition-slow);
}

.bar-user {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    background: linear-gradient(90deg, var(--color-primary), var(--color-secondary));
    transition: width var(--transition-slow);
    box-shadow: var(--glow-primary);
}

.bar-marker {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 3px;
    height: 100%;
    background: var(--color-text-primary);
}

/* Recommendations */
.recommendations {
    margin-bottom: var(--space-xl);
}

.recommendations-title {
    font-family: var(--font-display);
    font-size: var(--text-body-l);
    font-weight: 700;
    margin-bottom: var(--space-md);
    color: var(--color-text-primary);
}

.recommendation-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
}

.recommendation-item {
    display: flex;
    align-items: flex-start;
    gap: var(--space-md);
    padding: var(--space-md);
    background: var(--color-bg);
    border-left: 3px solid var(--color-primary);
}

.recommendation-icon {
    flex-shrink: 0;
    width: 30px;
    height: 30px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-primary);
    font-size: 1rem;
}

.recommendation-text {
    flex: 1;
    font-size: var(--text-body-m);
    color: var(--color-text-secondary);
    line-height: 1.6;
}

/* Products */
.products {
    margin-top: var(--space-xl);
}

.products-title {
    font-family: var(--font-display);
    font-size: var(--text-body-l);
    font-weight: 700;
    margin-bottom: var(--space-md);
    color: var(--color-text-primary);
}

.product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: var(--space-lg);
}

.product-card {
    background: var(--color-bg);
    padding: var(--space-lg);
    border: 1px solid var(--color-surface-light);
    transition: all var(--transition-fast);
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
}

.product-card:hover {
    border-color: var(--color-primary);
    transform: translateY(-4px);
    box-shadow: 0 8px 20px oklch(0% 0 0 / 0.3);
}

.product-image {
    width: 100%;
    aspect-ratio: 1;
    background: var(--color-surface);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3rem;
    color: var(--color-text-muted);
    overflow: hidden;
    position: relative;
}

.product-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform var(--transition-medium);
}

.product-card:hover .product-image img {
    transform: scale(1.05);
}

.product-name {
    font-weight: 600;
    font-size: var(--text-body-m);
    color: var(--color-text-primary);
}

.product-price {
    font-family: var(--font-display);
    font-size: var(--text-body-l);
    font-weight: 700;
    color: var(--color-primary);
}

/* ===================================
   Chatbot Styles
   =================================== */

/* Floating Chat Toggle Button */
.chat-toggle-btn {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    width: 60px;
    height: 60px;
    border-radius: 50%;
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    border: none;
    color: var(--color-bg);
    font-size: 1.5rem;
    cursor: pointer;
    box-shadow: var(--glow-primary);
    transition: all var(--transition-fast);
    z-index: 999;
    display: flex;
    align-items: center;
    justify-content: center;
}

.chat-toggle-btn:hover {
    transform: scale(1.1);
    box-shadow: 0 0 30px oklch(75% 0.19 155 / 0.5),
                0 0 60px oklch(75% 0.19 155 / 0.3);
}

.chat-badge {
    position: absolute;
    top: -8px;
    right: -8px;
    background: var(--color-error);
    color: white;
    padding: 0.25rem 0.5rem;
    border-radius: 12px;
    font-size: 0.7rem;
    font-weight: 600;
    white-space: nowrap;
    display: none;
}

/* Chatbot Widget */
.chatbot-widget {
    position: fixed;
    bottom: 2rem;
    right: 2rem;
    width: 400px;
    max-width: calc(100vw - 4rem);
    height: 600px;
    max-height: calc(100vh - 4rem);
    background: var(--color-surface);
    border: 2px solid var(--color-primary);
    border-radius: 12px;
    display: none;
    flex-direction: column;
    box-shadow: 0 0 40px oklch(0% 0 0 / 0.5);
    z-index: 1000;
    overflow: hidden;
}

.chatbot-widget.open {
    display: flex;
}

/* Chatbot Header */
.chatbot-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: var(--space-md);
    background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
    color: var(--color-bg);
}

.chatbot-header-content {
    display: flex;
    align-items: center;
    gap: var(--space-md);
}

.chatbot-avatar {
    width: 40px;
    height: 40px;
    background: var(--color-bg);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    color: var(--color-primary);
}

.chatbot-title h3 {
    font-family: var(--font-display);
    font-size: 1.1rem;
    margin: 0;
}

.chatbot-status {
    font-size: 0.8rem;
    opacity: 0.9;
    margin: 0;
}

.close-chat-btn {
    background: none;
    border: none;
    color: var(--color-bg);
    font-size: 1.2rem;
    cursor: pointer;
    padding: 0.5rem;
    transition: transform var(--transition-fast);
}

.close-chat-btn:hover {
    transform: scale(1.2);
}

/* Chatbot Messages */
.chatbot-messages {
    flex: 1;
    overflow-y: auto;
    padding: var(--space-md);
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    background: var(--color-bg);
}

.chat-message {
    display: flex;
    gap: var(--space-sm);
    align-items: flex-start;
    animation: messageSlideIn 0.3s ease;
}

@keyframes messageSlideIn {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.message-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 0.9rem;
}

.bot-message .message-avatar {
    background: var(--color-primary);
    color: var(--color-bg);
}

.user-message {
    flex-direction: row-reverse;
}

.user-message .message-avatar {
    background: var(--color-secondary);
    color: var(--color-bg);
}

.message-content {
    max-width: 75%;
    padding: var(--space-sm) var(--space-md);
    border-radius: 12px;
    font-size: var(--text-body-s);
    line-height: 1.5;
}

.bot-message .message-content {
    background: var(--color-surface);
    color: var(--color-text-primary);
    border-bottom-left-radius: 4px;
}

.user-message .message-content {
    background: var(--color-primary);
    color: var(--color-bg);
    border-bottom-right-radius: 4px;
}

.message-content p {
    margin: 0 0 0.5rem 0;
}

.message-content p:last-child {
    margin-bottom: 0;
}

.message-content ul {
    margin: 0.5rem 0;
    padding-left: 1.25rem;
}

.message-content li {
    margin: 0.25rem 0;
}

/* Typing Indicator */
.typing-indicator {
    display: flex;
    gap: 4px;
    padding: var(--space-sm);
    background: var(--color-surface);
    border-radius: 12px;
    width: fit-content;
    margin-left: 44px;
}

.typing-indicator span {
    width: 8px;
    height: 8px;
    background: var(--color-primary);
    border-radius: 50%;
    animation: typing 1.4s infinite;
}

.typing-indicator span:nth-child(2) {
    animation-delay: 0.2s;
}

.typing-indicator span:nth-child(3) {
    animation-delay: 0.4s;
}

@keyframes typing {
    0%, 60%, 100% {
        transform: translateY(0);
        opacity: 0.5;
    }
    30% {
        transform: translateY(-10px);
        opacity: 1;
    }
}

/* Chatbot Input Area */
.chatbot-input-area {
    padding: var(--space-md);
    background: var(--color-surface);
    border-top: 1px solid var(--color-surface-light);
}

.chat-suggestions {
    display: flex;
    gap: var(--space-sm);
    margin-bottom: var(--space-sm);
    flex-wrap: wrap;
}

.suggestion-btn {
    padding: var(--space-xs) var(--space-sm);
    background: var(--color-bg);
    border: 1px solid var(--color-primary);
    color: var(--color-primary);
    border-radius: 16px;
    font-size: 0.75rem;
    cursor: pointer;
    transition: all var(--transition-fast);
    white-space: nowrap;
}

.suggestion-btn:hover {
    background: var(--color-primary);
    color: var(--color-bg);
}

.chatbot-input-container {
    display: flex;
    gap: var(--space-sm);
    align-items: center;
}

.chat-input {
    flex: 1;
    padding: var(--space-sm);
    background: var(--color-bg);
    border: 1px solid var(--color-surface-light);
    border-radius: 8px;
    color: var(--color-text-primary);
    font-size: var(--text-body-s);
    font-family: var(--font-body);
}

.chat-input:focus {
    outline: none;
    border-color: var(--color-primary);
}

.send-chat-btn {
    width: 40px;
    height: 40px;
    background: var(--color-primary);
    border: none;
    border-radius: 8px;
    color: var(--color-bg);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all var(--transition-fast);
}

.send-chat-btn:hover {
    background: var(--color-secondary);
    transform: scale(1.05);
}

.send-chat-btn:active {
    transform: scale(0.95);
}

/* ===================================
   Responsive Design
   =================================== */
@media (max-width: 768px) {
    :root {
        --gutter: 1rem;
        --space-3xl: 3rem;
    }

    .hero-container {
        padding: var(--space-lg);
    }

    .hero-icon {
        width: 60px;
        height: 60px;
        font-size: 2rem;
    }

    .cta-button {
        padding: var(--space-md) var(--space-xl);
        width: 100%;
        max-width: 300px;
    }

    .quiz-header {
        gap: var(--space-md);
    }

    .answer-option {
        padding: var(--space-md);
    }

    .category-result {
        padding: var(--space-lg);
    }

    .category-result-header {
        flex-direction: column;
        align-items: flex-start;
    }

    .category-emissions {
        text-align: left;
    }

    .product-grid {
        grid-template-columns: 1fr;
    }

    /* Chatbot Responsive */
    .chatbot-widget {
        width: calc(100vw - 2rem);
        height: calc(100vh - 2rem);
        right: 1rem;
        bottom: 1rem;
    }

    .chat-toggle-btn {
        right: 1rem;
        bottom: 1rem;
    }
}

@media (max-width: 480px) {
    .progress-text {
        font-size: var(--text-caption);
    }

    .category-badge {
        padding: var(--space-xs) var(--space-md);
        font-size: var(--text-caption);
    }

    .results-icon {
        width: 70px;
        height: 70px;
        font-size: 2rem;
    }

    .chat-badge {
        font-size: 0.6rem;
        padding: 0.2rem 0.4rem;
    }
}

/* ===================================
   Ultra-wide Support
   =================================== */
@media (min-width: 2000px) {
    :root {
        --max-width: 1800px;
    }
}

/* ===================================
   Accessibility
   =================================== */
@media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
}

/* Focus styles */
button:focus-visible,
.answer-option:focus-visible {
    outline: 2px solid var(--color-primary);
    outline-offset: 4px;
}

/* ===================================
   Print Styles
   =================================== */
@media print {
    .background-grid,
    .glow-orb,
    .chat-toggle-btn,
    .chatbot-widget {
        display: none;
    }

    body {
        background: white;
        color: black;
    }
}
